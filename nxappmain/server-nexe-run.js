define(['nxappmain/serverbuild'],function(all){
    var _r = require;
    _r(['nxappmain/main_server'],function(e){

    });

    //var _e = require('xide/types/Types');
    //console.dir(require.cache['xide/types/Types']);
    //console.dir(xide.types);

    /*
    var t = define(['nxapp/utils/FileUtils'],function(winston){
        console.log('file manager ready',winston);
        //console.dir(utils);
    });
    */

    /*
    var t = require(['nxappmain/main_xide'],function(winston){
        console.log('file manager ready',winston);
        //console.dir(utils);
    });
    */

    //console.dir(require.cache);
});
